# include <stdio.h> // 预处理指令，包含另一个文件
int main(void) //总是第一个被调用的函数
{
	int num;
	num = 1;
	printf("I am a simple ");
	printf("computer.\n");
	printf("My favorite number is %d because it is firtst.\n", num);

	return 0;
}
